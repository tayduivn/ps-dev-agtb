describe("Router", function() {
    var app, router;

    beforeEach(function() {
        app = SugarTest.app;
        router = app.router;
    });

    it("should extend the default router", function(){

    });

    it("should replace the router in app with the newly extended one", function(){

    });

    describe("when extending default router" , function() {
        it("should override existing methods with the new ones of the same name", function(){

        });

        it("should add new methods that are not present in the existing router", function(){

        });

        it("should override existing routes with the new ones of the same name", function(){

        });

        it("should add new routes that are not present in the existing router", function(){

        });
    });
});