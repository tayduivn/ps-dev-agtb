<soda>
	<puts text="Test: Accounts_0063.xml starting." />

	<!--Create a account record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Create_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Contact_Create_Lib.xml" />


	<!--Search the record-->
	<link id="moduleTab_Accounts" />
	<div class="moduleTitle">
		<link href="index.php?module=Accounts&amp;action=index" click="false" />
	</div>
	<textfield id="name_basic" set="{@pre_account_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_account_name}" />
	</table>
	<wait timeout="5" />
	<!--Go its detail view-->
	<browser assert="{@pre_account_name}" />

	<!--Goto contact subpanel -->
	<button id="accounts_contacts_select_button" />
	<wait timeout="5" />
	<textfield id="first_name_advanced" set="{@pre_contact_first_name}" />
	<textfield id="last_name_advanced" set="{@pre_contact_last_name}" />
	<button value="Search" />
	<wait timeout="5" />
	<!--Assert the record in the list view-->
	<link text="{@pre_contact_first_name} {@pre_contact_last_name}" />


	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Contact_Delete_Lib.xml" />

	<puts text="Test: Accounts_0063.xml finished." />

</soda>
