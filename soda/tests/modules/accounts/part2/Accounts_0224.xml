<soda>
	<!--
		Account Detail - Opportunities sub-panel - Create _Verify that new
		opportunities is correctly "Select" on "Opportunities" sub-panel.
	-->
	<puts text="Test: Accounts_0224.xml starting." />

	<!-- Creat an Opportunity -->
	<script file="{@global.scriptsdir}/modules/lib/Opp_Create_Lib.xml" />

	<!--Create a account record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Create_Lib.xml" />

	<!--Search the record-->
	<link id="moduleTab_Accounts" />
	<div class="moduleTitle">
		<link href="index.php?module=Accounts&amp;action=index" click="false" />
	</div>
	<textfield id="name_basic" set="{@pre_account_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_account_name}" />
	</table>

	<!-- Select created Opp via the Opportunity subpanel -->
	<div id="list_subpanel_opportunities">
		<button id="accounts_opportunities_select_button" />
	</div>
	<table class="list view">
		<link text="{@pre_opp_name}" />
	</table>

	<!-- Verify Opp appears properly on subpanel -->
	<div id="list_subpanel_opportunities">
		<browser assert="{@pre_opp_name}" />
	</div>

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Opp_Delete_Lib.xml" />

	<puts text="Test: Accounts_0224.xml finished." />
</soda>
