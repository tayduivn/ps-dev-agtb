<soda>
	<!--
		Create Account _Verify that error message "Missing required field: Name" is
		displayed when creating new account with negative/garbage data.
	-->
	<puts text="Test: Accounts_0314.xml starting." />
	<timestamp />

	<link id="moduleTab_Accounts" />
	<div class="moduleTitle">
		<link href="index.php?module=Accounts&amp;action=index" click="false" />
	</div>
	<csv file="{@global.scriptsdir}/csvs/accounts/Accounts_0314.csv" var="account_record_0314">
		<link text="Create Account" />
		<browser assert="Create" />
		<textfield id="name" set="{@account_record_0314.name}" />

		<button value="Save" />
		<browser assert="{@account_record_0314.name}" />
	</csv>

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
	<puts text="Test: Accounts_0314.xml finished." />
</soda>
