<soda>
	<!--
		Create Account _Verify that error message "Missing required field: Name" is
		displayed when creating new account with mandatory fields blank.
	-->
	<puts text="Test: Accounts_0315.xml starting." />
	<timestamp />

	<link id="moduleTab_Accounts" />
	<browser assert="Accounts" />
	<link text="Create Account" />
	<browser assert="Create" />
	<textfield id="name" set="" />
	<button value="Save" />
	<browser assert="Missing required field: Name" />
	<button value="Cancel" alert="true" />

	<puts text="Test: Accounts_0315.xml finished." />
</soda>
