<soda>
	<!--
		"Missing required fields: Subject" is displayed when creating an task related
		to an account in in-line form without mandatory field filled.
	-->
	<puts text="Test: Accounts_0377.xml starting." />

	<!--Create a account record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Create_Lib.xml" />

	<!--Search the record-->
	<link id="moduleTab_Accounts" />
	<div class="moduleTitle">
		<link href="index.php?module=Accounts&amp;action=index" click="false" />
	</div>
	<textfield id="name_basic" set="{@pre_account_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_account_name}" />
	</table>

	<button name="Activities_createtask_button" />
	<button id="Tasks_subpanel_save_button" />
	<browser assert="Missing required field: Subject" />
	<link id="moduleTab_Accounts" alert="true"/>
	<div class="moduleTitle">
		<link href="index.php?module=Accounts&amp;action=index" click="false" />
	</div>

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
	<puts text="Test: Accounts_0377.xml finished." />
</soda>
