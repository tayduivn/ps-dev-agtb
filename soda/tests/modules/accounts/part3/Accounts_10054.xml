<soda>
	<puts text="Test: Accounts_10054.xml starting." />
	
	<script file="{@global.scriptsdir}/modules/lib/Account_Create_Lib.xml" />
	<link id="moduleTab_Accounts" />
	
	<browser assert="{@account_record.name} {@user.name} {@stamp}" />
	<link text="{@account_record.assigned_user_name}" />
	
	<browser assert="Employees" />
	<browser assert="{@account_record.assigned_user_name}" />

	<script file="{@global.scriptsdir}/modules/lib/Account_Delete_Lib.xml" />
		
	<puts text="Test: Accounts_10054.xml finished." />
	
</soda>