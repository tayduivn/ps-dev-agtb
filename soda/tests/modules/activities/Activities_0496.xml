<soda>
	<!-- Edit Note_Verify that removing attachment for a note is canceled. -->
	<puts text="Test: Activities_0496.xml starting" />

	<script file="{@global.scriptsdir}/modules/activities/Activities_0489.xml" />

	<link id="moduleTab_Notes" />
	<browser assert="Notes" />
	<link text="View Notes" />
	<button id="search_form_clear" />
	<textfield id="name_basic" set="{@pre_note_name_0489}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_note_name_0489}" />
	</table>

	<browser assert="{@pre_note_name_0489}" />
	<button id="edit_button" />
	<button value="Remove" />
	<button value="Cancel" alert="true" />
	<browser assert="{@pre_note_name_0489}" />


	<script file="{@global.scriptsdir}/modules/lib/Note_Delete_Lib.xml" />
	<puts text="Test: Activities_0496.xml finished" />
</soda>