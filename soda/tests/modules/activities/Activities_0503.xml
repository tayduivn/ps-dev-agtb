<soda>
	<!--
		Delete Note_Verify that deleting a note from detail view can be canceled.
	-->
	<puts text="Test: Activities_0503.xml starting" />


	<!--create call record-->
	<script file="{@global.scriptsdir}/modules/lib/Note_Create_Lib.xml" />

	<link id="moduleTab_Notes" />
	<browser assert="Notes" />
	<link text="View Notes" />

	<button id="search_form_clear" />
	<textfield id="name_basic" set="{@pre_note_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_note_name}" />
	</table>


	<button name="Delete" alert="false" />

	<browser assert="{@pre_note_name}" />
	<script file="{@global.scriptsdir}/modules/lib/Note_Delete_Lib.xml" />

	<puts text="Test: Activities_0503.xml finished" />
</soda>