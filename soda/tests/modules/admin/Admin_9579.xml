<soda>
	<!-- Verify the special label in export setting -->
	<puts text="Test: Admin_9579.xml starting." />
	<link text="Admin" />
	<div class="moduleTitle">
		<browser assert="Administration" />
	</div>

	<link text="Locale" />
	<div class="moduleTitle">
		<browser assert="Administration" />
		<browser assert="Locale" />
	</div>
	<table class="edit view" index="3">
		<browser assert="Default Character Set for Import and Export" />
		<select name="default_export_charset" assert="UTF-8" />
	</table>

	<puts text="Test: Admin_9579.xml finished" />
</soda>
