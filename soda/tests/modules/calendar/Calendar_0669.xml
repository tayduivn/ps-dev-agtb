<soda>
	<!-- Verify that call can be scheduled at a specified time -->
	<puts text="Test: Calendar_0669.xml starting." />
	<csv file="{@global.scriptsdir}/csvs/calendar/CalendarAssertText.csv" var="calendar" />

	<link id="moduleTab_Calendar" />
	<link text="09:00" />

	<timestamp/>

	<table id="daily_cal_table">
		<div id="9_appt">
			<radio name="appointment" index="1" set="true" />
			<textfield name="name" set="call_record{@stamp}" />
			<button name="button" />
		</div>
	</table>
	<div class="moduleTitle">
		<link href="index.php?module=Calls&amp;action=index" click="false" />
		<browser assert="call_record{@stamp}" />
	</div>

	<puts text="Test: Calendar_0669.xml finished." />
</soda>