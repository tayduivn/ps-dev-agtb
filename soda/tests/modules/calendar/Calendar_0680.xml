<soda>
	<!-- Verify that scheduled calls are searched with basic search condition -->
	<puts text="Test: Calendar_0680.xml starting." />
	<csv file="{@global.scriptsdir}/csvs/calendar/CalendarAssertText.csv" var="calendar" />

	<!--Create a call record/-->
	<script file="{@global.scriptsdir}/modules/lib/Call_Create_Lib.xml" />

	<div id="Calls">
		<link text="View Calls" />
	</div>
	<div class="moduleTitle">
		<link href="index.php?module=Calls&amp;action=index" click="false" />
	</div>

	<form id="search_form">
		<button name="clear" />
		<textfield id="name_basic" set="{@pre_call_name}" />
		<button value="Search" />
	</form>

	<form id="MassUpdate">
		<link text="{@pre_call_name}" click="false" exist="true" />
		<span class="pageNumbers" assert="(1 - 1 of 1)" />
	</form>

	<script file="{@global.scriptsdir}/modules/lib/Call_Delete_Lib.xml" />

	<puts text="Test: Calendar_0680.xml finished." />
</soda>