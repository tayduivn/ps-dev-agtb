<soda>
	<!-- Edit Case_Verify that case is not changed when several mandatory fields are left empty for the "Edit" function -->
	<puts text="Test: Cases_1332.xml starting." />
	<csv file="{@global.scriptsdir}/csvs/cases/CasesAssertText.csv" var="cases" />
	<timestamp/>

	<!--Create a case record-->
	<script file="{@global.scriptsdir}/modules/lib/Case_Create_Lib.xml" />

	<!--go to case module and edit-->
	<link id="moduleTab_Cases" />
	<div class="moduleTitle">
		<link href="index.php?module=Cases&amp;action=index" click="false" />
	</div>

	<textfield id="name_basic" set="{@pre_case_name}" />
	<button id="search_form_submit" />

	<link text="{@pre_case_name}" />

	<button id="edit_button" />

	<!--create case record from quick create-->
	<button id="btn_clr_account_name" />
	<textfield id="name" clear="true" />
	<button value="Save" />
	<browser assert="Missing required field: Account Name" />
	<browser assert="Missing required field: No match for field: Account Name" />
	<browser assert="Missing required field: Subject" />

	<link id="moduleTab_Cases" alert="true" />
	<div class="moduleTitle">
		<link href="index.php?module=Cases&amp;action=index" click="false" />
	</div>

	<textfield id="name_basic" set="{@pre_case_name}" />
	<button id="search_form_submit" />

	<link text="{@pre_case_name}" />

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Case_Delete_Lib.xml" />

	<puts text="Test: Cases_1332.xml finished." />
</soda>