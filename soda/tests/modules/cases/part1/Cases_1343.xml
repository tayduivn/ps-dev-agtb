<soda>
	<!-- Edit Contact_Verify that the information of a contact for case can be modified when using "Edit" function in "Contacts" sub-panel -->
	<puts text="Test: Cases_1343.xml starting." />
	<csv file="{@global.scriptsdir}/csvs/cases/CasesAssertText.csv" var="cases" />

	<!--Create a case record-->
	<script file="{@global.scriptsdir}/modules/lib/Case_Create_Lib.xml" />
	<var var="case_record" set="{@pre_case_name}" />

	<!--add contact to case-->
	<link id="moduleTab_Cases" />
	<div class="moduleTitle">
		<link href="index.php?module=Cases&amp;action=index" click="false" />
	</div>

	<button value="Clear" />
	<textfield id="name_basic" set="{@case_record}" />
	<button id="search_form_submit" />
	<link text="{@case_record}" />

	<csv file="{@global.scriptsdir}/csvs/cases/Case_1343.csv" var="Case_1343">
		<button id="contacts_cases_create_button" />
		<wait timeout="2" />
		<textfield id="last_name" set="{@Case_1343.last_name}" />
		<button id="Contacts_subpanel_save_button" />

		<div id="list_subpanel_contacts">
			<table class="list view">
				<link text="{@Case_1343.last_name}" click="false" />
			</table>

			<link class="listViewTdToolsS1" index="1" alert="true" />
		</div>

		<div class="moduleTitle">
			<link href="index.php?module=Contacts&amp;action=index" click="false" />
			<browser assert="{@Case_1343.last_name}" />
		</div>
		<textfield name="last_name" set="{@Case_1343.new_name}" />
		<button value="Save" />
	</csv>  
	<div id="list_subpanel_contacts">
		<table class="list view">
			<link text="{@Case_1343.new_name}" click="false" />
		</table>
	</div>

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Case_Delete_Lib.xml" />

	<puts text="Test: Cases_1343.xml finished." />
</soda>