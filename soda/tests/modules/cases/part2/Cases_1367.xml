<soda>
	<!-- Schedule Call_Verify that call is not scheduled in "Activities" sub-panel when using "Cancel" function -->
	<puts text="Test: Cases_1367.xml starting." />
	<csv file="{@global.scriptsdir}/csvs/cases/CasesAssertText.csv" var="cases" />

	<!--Create a case record-->
	<script file="{@global.scriptsdir}/modules/lib/Case_Create_Lib.xml" />
	<var var="case_record" set="{@pre_case_name}" />

	<timestamp />

	<!--go to the case-->
	<link id="moduleTab_Cases" />
	<div class="moduleTitle">
		<link href="index.php?module=Cases&amp;action=index" click="false" />
	</div>

	<button value="Clear" />
	<textfield id="name_basic" set="{@case_record}" />
	<button id="search_form_submit" />
	<link text="{@case_record}" />

	<!--create Call-->
	<div id="list_subpanel_activities">
		<button value="  Log Call  " />
      <wait timeout="3" />
	</div>

	<csv file="{@global.scriptsdir}/csvs/cases/Case_1367.csv" var="Case_1367">
      <div id="Default_Calls_Subpanel">
   		<textfield id="name" set="{@Case_1367.name} {@user.name} {@stamp}" />
      </div>
		<button id="Calls_subpanel_cancel_button" alert="true" />
	</csv>
	<div id="list_subpanel_activities">
		<span class="pageNumbers" assert="(0 - 0 of 0)" />
	</div>

	<!--Delete the record-->
	<script file="{@global.scriptsdir}/modules/lib/Case_Delete_Lib.xml" />

	<puts text="Test: Cases_1367.xml finished." />
</soda>
