<soda>
	<!-- Create_account_in_selecting_accounts_pop-up_window_in_case_edit_view -->
	<puts text="Test: Cases_8251.xml starting." />
	<csv file="{@global.scriptsdir}/csvs/cases/CasesAssertText.csv" var="cases" />

	<!--go to the case-->
	<link id="moduleTab_Cases" />
	<div class="moduleTitle">
		<link href="index.php?module=Cases&amp;action=index" click="false" />
	</div>

	<link text="Create Case" />

	<button id="btn_account_name" />
	<browser assert="Account Name" />
	<browser assert="City" />
	<browser assert="State" />
	<browser assert="Country" />
	<browser assert="User" />

	<browser send_keys="Ctrl+W" />
	<attach title="/SugarCRM/">
		<wait timeout="1" />
	</attach>

	<puts text="Test: Cases_8251.xml finished." />
</soda>
