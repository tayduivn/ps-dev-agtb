<soda>
	<!--
		Create task_Verify that related task can be created by "Full Form" function
		from contact detail view.
	-->
	<puts text="Test: Contacts_1471.xml starting." />
	<timestamp />
	<!--Create contact record-->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Create_Lib.xml" />

	<!--Search the Contact record-->
	<link id="moduleTab_Contacts" />
	<div class="moduleTitle">
		<link href="index.php?module=Contacts&amp;action=index" click="false" />
	</div>
	<link text="/Basic Search/" required="false" />
	<button id="search_form_clear" />
	<textfield id="search_name_basic" set="{@pre_contact_first_name}" />
	<button id="search_form_submit" />
	<wait timeout="3" />
	<table class="list view">
		<link text="{@pre_contact_first_name} {@pre_contact_last_name}" />
	</table>

	<browser assert="{@pre_contact_first_name} {@pre_contact_last_name}" />
	<button name="Activities_createtask_button" />
	<button id="Tasks_subpanel_full_form_button" />
	<!--goto task subpanel & click "Full Form"-->
	<csv file="{@global.scriptsdir}/csvs/contacts/Contacts_1471.csv" var="contact_record_1471">
		<textfield id="name" set="{@contact_record_1471.name} {@user.name} {@stamp}" />
		<select id="priority" set="{@contact_record_1471.priority}" />
		<select id="status" set="{@contact_record_1471.status}" />
		<textfield id="description" set="{@contact_record_1471.description}" />

		<!--Due Date-->
		<textfield id="date_due_date" set="{@contact_record_1471.date_due_date}" />
		<select id="date_due_hours" set="{@contact_record_1471.date_due_hours}" />
		<select id="date_due_minutes" set="{@contact_record_1471.date_due_minutes}" />
		<checkbox id="date_due_flag" set="{@contact_record_1471.date_due_flag}" />

		<!--Start Date-->
		<textfield id="date_start_date" set="{@contact_record_1471.date_start_date}" />
		<select id="date_start_hours" set="{@contact_record_1471.date_start_hours}" />
		<select id="date_start_minutes" set="{@contact_record_1471.date_start_minutes}" />
		<checkbox id="date_start_flag" set="{@contact_record_1471.date_start_flag}" />

		<!--user select-->
		<button id="btn_assigned_user_name" />
		<browser assert="User Search" />
		<textfield id="last_name_advanced" set="{@contact_record_1471.assigned_user_name}" />
		<button value="Search" />
		<link text="{@contact_record_1471.assigned_user_name}" />

		<!--teams select-->
		<table id="EditView_team_name_table">
			<button value="Select" />
		</table>
		<browser assert="Team Search" />
		<textfield name="name" set="{@contact_record_1471.team_name}" />
		<button value="Search" />
		<link text="{@contact_record_1471.team_name}" />

		<button value="Save" />

		<!--Verify the task record created in subpanel-->
		<div id="list_subpanel_activities">
			<table class="list view">
				<link text="{@contact_record_1471.name} {@user.name} {@stamp}" click="false" />
			</table>
		</div>
	</csv>

	<!--Delete contact record-->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Delete_Lib.xml" />
	<puts text="Test: Contacts_1471.xml finished." />
</soda>
