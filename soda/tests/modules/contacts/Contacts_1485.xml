<soda>
	<!--
		Delete note or attachment_Verify that a related note or attachment can be
		deleted from contact detail view.
	-->
	<puts text="Test: Contacts_1485.xml starting." />

	<!--Create contact record-->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Create_Lib.xml" />

	<!--Search the Contact record-->
	<link id="moduleTab_Contacts" />
	<div class="moduleTitle">
		<link href="index.php?module=Contacts&amp;action=index" click="false" />
	</div>
	<button id="search_form_clear" />
	<textfield id="search_name_basic" set="{@pre_contact_first_name}" />
	<button id="search_form_submit" />
	<table class="list view">
		<link text="{@pre_contact_first_name} {@pre_contact_last_name}" />
	</table>

	<!--Create Note/Attachment via subpanel-->
	<csv file="{@global.scriptsdir}/csvs/contacts/Contacts_1483.csv" var="contact_note_1485">
		<div id="list_subpanel_history">
			<button value="  Create Note or Attachment  " />
		</div>

		<wait timeout="3" />
		<textfield id="name" set="{@contact_note_1485.name}" />
		<textfield id="name" set="{@contact_note_1485.name}" />

		<form id="form_SubpanelQuickCreate_Notes">
			<button value="Save" />
		</form>
	</csv>
	<wait timeout="10" />

	<!-- Verify the creation of the Note/Attachment -->
	<div id="list_subpanel_history">
		<link text="{@contact_note_1485.name}" click="false" />
	</div>

	<!-- "rem" Note/Attachment via "rem" button in subpanel -->
	<div id="list_subpanel_history">
		<link class="listViewTdToolsS1" index="2" alert="true" />
	</div>
	<wait timeout="3" />

	<!-- Verify "rem" -->
	<div id="list_subpanel_history">
		<link text="{@contact_note_1485.new_name}" exist="false" />
	</div>

	<!--Delete records-->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Note_Delete_Lib.xml" />
	<puts text="Test: Contacts_1485.xml finished." />
</soda>
