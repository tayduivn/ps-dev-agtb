<soda>
	<!--
		Select lead_Verify that a related lead can be selected into a contact from
		contact detail view.
	-->
	<puts text="Test: Contacts_1555.xml starting." />

	<!-- Create Records -->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Create_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Lead_Create_Lib.xml" />

	<!-- Search for contact record -->
	<link id="moduleTab_Contacts" />
	<div class="moduleTitle">
		<link href="index.php?module=Contacts&amp;action=index" click="false" />
	</div>
	<form id="search_form">
		<button name="clear" />
		<textfield id="search_name_basic" set="{@pre_contact_first_name}" />
		<button id="search_form_submit" />
	</form>

	<table class="list view">
		<link text="{@pre_contact_first_name} {@pre_contact_last_name}" />
	</table>

	<div class="moduleTitle">
		<link href="index.php?module=Contacts&amp;action=index" click="false" />
		<browser assert="{@pre_contact_first_name} {@pre_contact_last_name}" />
	</div>

	<!-- Select Lead via subpanel -->
	<button name="contact_leads_select_button" />

	<textfield id="first_name_advanced" set="{@pre_lead_first_name}" />
	<button value="Search" />
	<wait timeout="3" />

	<table class="list view">
		<link text="{@pre_lead_salutation} {@pre_lead_first_name} {@pre_lead_last_name}" />
	</table>
	<wait timeout="3" />

	<!-- Verify select of Lead worked -->
	<div id="list_subpanel_leads">
		<link text="{@pre_lead_salutation} {@pre_lead_first_name} {@pre_lead_last_name}"
			click="false" />
	</div>

	<!-- Delete Records -->
	<script file="{@global.scriptsdir}/modules/lib/Contact_Delete_Lib.xml" />
	<script file="{@global.scriptsdir}/modules/lib/Lead_Delete_Lib.xml" />
	<puts text="Test: Contacts_1555.xml finished." />
</soda>
