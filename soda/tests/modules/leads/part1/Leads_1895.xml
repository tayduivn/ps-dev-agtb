<soda>
	<!--
		Create Lead_Verify that lead is not duplicated when using cancel function.
	-->
	<puts text="Test: Leads_1895.xml starting" />

	<script file="{@global.scriptsdir}/modules/lib/Lead_Create_Lib.xml" />

	<link id="moduleTab_Leads" />
	<browser assert="Leads" />

	<!--Select a lead record-->
	<browser assert="Leads" />
	<textfield id="search_name_basic" set="{@pre_lead_last_name}" />
	<button id="search_form_submit" />
	<link
		text="{@Lead_New_Record.salutation} {@pre_lead_first_name} {@pre_lead_last_name}" />


	<button id="duplicate_button" />
	<browser assert="Edit" />
	<button value="Cancel" alert="true" />

	<browser
		assert="{@Lead_New_Record.salutation} {@pre_lead_first_name} {@pre_lead_last_name}" />

	<!--Delete the record-->

	<script file="{@global.scriptsdir}/modules/lib/Lead_Delete_Lib.xml" />
	<puts text="Test: Leads_1895.xml starting" />
</soda>
