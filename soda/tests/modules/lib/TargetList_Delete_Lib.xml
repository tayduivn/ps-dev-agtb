<soda>
	<puts text="Lib: TargetList_Delete_Lib.xml starting." />

	<!-- Nav to campaigns module. -->
	<link href="index.php?module=Campaigns&amp;action=index" />
	<browser assert="Campaigns" />

	<link text="View Target Lists" />
	
	<!-- Clear the search criteria. -->
	<browser assert="My Items" />

	<button id="search_form_clear" />
	<button id="search_form_submit" />

	<!-- Delete all test records. -->
	<checkbox name="massall" required="false" timeout="2" set="true" />
	<link text="Actions"/>
	<link text="Delete" alert="true" />

	<!-- Assert no test records exist. -->
	<span class="pageNumbers" assert="(0 - 0 of 0)" />

	<puts text="Lib: TargetList_Delete_Lib.xml finished." />
</soda>