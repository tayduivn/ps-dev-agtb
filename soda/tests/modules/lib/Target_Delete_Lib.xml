<soda>
	<puts text="Lib: Target_Delete_Lib.xml starting." />
	<csv file="{@global.scriptsdir}/csvs/campaigns/CampaignAssertText.csv" var="campaigns"/>

	<!--Nav to campaigns module -->
	<link href="index.php?module=Campaigns&amp;action=index" />
	
	<link text="View Targets" />
	
	<!-- Clear the search criteria. -->
	<button id="search_form_clear" />
	<button id="search_form_submit" />

	<!--delete all test record -->
	<checkbox name="massall" click="true" />
	<link text="Actions" />
	<link text="Delete" alert="true" />

	<!--Assert no test record exists-->
	<span class="pageNumbers" assert="(0 - 0 of 0)" />

	<puts text="Lib: Target_Delete_Lib.xml starting." />
</soda>