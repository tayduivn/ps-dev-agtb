<soda>
	<puts text="Lib: login.xml starting." />
	<csv file="{@global.scriptsdir}/csvs/users.csv" var="user" />

	<browser url="{@global.url}" />

	<link text="Log Out" required="false" timeout="2" />
	<browser assert="Please enter your user name and password" />

	<textfield id="user_name" set="{@user.name}" default="admin" />
	<textfield id="user_password" set="{@user.password}" default="admin" />
	<button id="login_button" />

   <!-- Match a new user login or an existing user login, but not a failed login. -->
	<browser assert="/Welcome(, | to Sugar!)/" />

	<puts text="Lib: login.xml finished." />
</soda>
