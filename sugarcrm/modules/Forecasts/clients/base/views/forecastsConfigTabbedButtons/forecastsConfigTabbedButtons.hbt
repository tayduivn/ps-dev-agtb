<div class="modal-navigation setup">
    <ul class="nav nav-tabs">
        {{#each breadCrumbLabels}}
            <li><a href="#" data-index="{{index}}">{{str label "Forecasts"}}</a></li>
        {{/each}}
    </ul>
</div>

{{#each meta.panels}}
<div class="modal-footer">
    {{#each buttons}}
        {{field ../../this ../../model}}
    {{/each}}
</div>
{{/each}}
